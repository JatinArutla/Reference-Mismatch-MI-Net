# Phase 1: CSP+LDA calibration and mismatch matrix.
# MOABB is pinned for reproducibility of the 65.99% published benchmark.
moabb==1.5.0
mne>=1.5
pyriemann>=0.5
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Runtime display: progress bar during run_mismatch, heatmap in plot_mismatch_matrix.
tqdm>=4.60
matplotlib>=3.5

# Testing
pytest>=7.0

# Phase 2 (uncomment when DL experiments start):
# braindecode>=0.8
# torch>=2.0
# skorch>=0.15
