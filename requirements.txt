# Pinned: the IV-2a CSP+LDA calibration target (65.99%) was verified on moabb
# 1.5.0, and braindecode needs mne >= 1.11 to import its preprocessing classes.
moabb==1.5.0
mne>=1.11
braindecode>=1.0
torch>=2.0
skorch>=0.15
pyriemann>=0.5
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
scipy>=1.10
tqdm>=4.60
pytest>=7.0
