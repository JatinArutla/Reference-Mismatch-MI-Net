protocols:
  mechanistic:
    order:
      - apply_reference_transform
      - per_trial_per_channel_zscore_over_time
    details:
      per_trial: true
      per_channel: true
      axis: time

  deployment:
    order:
      - apply_reference_transform
      - fit_train_statistics_on_training_split_only
      - apply_same_statistics_to_train_and_test
    fixed_reference_training:
      stats_fit_reference: train_reference_only
    jitter_training:
      stats_fit_reference: mixed_reference_training_tensor
